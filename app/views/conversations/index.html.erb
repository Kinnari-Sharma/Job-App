<div class="mailbox">
  <h3>Mailbox</h3>
  <% a = 0 %>
  <div class="ui list">
    <div class = "item">
      <ul>
      <% @conversations.each do |conversation| %>
        <% if conversation.sender_id == current_user.id ||
              conversation.recipent_id == current_user.id %>

          <% if conversation.sender_id == current_user.id %>
            <% recipent = User.find(conversation.recipent_id) %>
          <% else %>
            <% recipent = User.find(conversation.sender_id) %>
          <% end %>
          <% a = 1 %>
          <li><%= link_to recipent.name,   conversation_messages_path(conversation)%></li>
        <% end %>
      <% end %>
      </ul>
      <% if a==0 %>
        <p>You have no messages!</p>
      <% end %>
    </div>
  </div>
</div>


<!--
<div class=”ui segment”>
  <h3>All Users</h3>
  <div class=”ui list”>
   <% @users.each do |user| %>
    <% if user.id != current_user.id && user.role != "admin" %>
     <div class=”item”>
        <%= user.name %> <%= link_to "Message me!", conversations_path(sender_id: current_user.id, recipent_id: user.id), method: :post%>
      </div>
    <% end %>
  <% end %>
  </div>
</div>

->