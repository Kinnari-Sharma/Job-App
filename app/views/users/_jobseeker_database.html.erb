
<ul class = "users">
  <% @users.each do |user| %>
		<% if user.role == "job-seeker" %>	
		  <li>
  			<%= gravatar_for user, size: 50 %>
  			<%= link_to user.name, user %>
  		</li>
		<% end %>
  <% end %>
</ul>
<%= will_paginate @users %>
